resources:
  pipelines:
    pl_mobills:
      name: Pipeline ETL Mobills
      libraries:
        - glob:
            include: ../src/mobills_etl/**
      catalog: "${var.catalog}"
      channel: CURRENT
      continuous: false
      photon: true
      root_path: ../src/mobills_etl
      schema: bronze
      serverless: true
      development: "${var.flag_dev}"
