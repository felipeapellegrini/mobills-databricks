catalogs:
  - name: mobills_prod

    grants:
      - principal: "data_engineers"
        privileges:
          - USE_CATALOG

schemas:
  - catalog: mobills_prod
    name: bronze
    grants:
      - principal: "data_engineers"
        privileges:
          - USE_SCHEMA
          - CREATE_TABLE
  - catalog: mobills_prod
    name: silver
    grants:
      - principal: "data_engineers"
        privileges:
          - USE_SCHEMA
          - SELECT
          - MODIFY
